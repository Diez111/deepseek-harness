{"id":"t24-int-to-words","difficulty":"deep","type":"bug-focal","origin":"mutation","status":"pass","wallMs":21752,"oracleOut":"PASS\n","oracleErr":"","agentTail":"     scale += 1\n\n    return \" \".join(reversed(parts))\n```\n\n- A `_hundreds` helper handles 0–999 (standard English: \"twelve\", \"twenty three\", \"one hundred fifteen\").\n- The main loop splits into three-digit groups with `thousand`/`million` scale words, skipping empty groups (e.g. `100100` → \"one hundred thousand one hundred\").\n- `0` returns `\"zero\"` via a special case.\n\nVerified: all provided examples pass, and `to_words` matches an independent reference implementation for every value `0..999_999` (and `999_999_999` and adjacent edges work too). No hyphenation is used, matching the requirement.\n"}
DONE
